{% comment %}
  ============================================================
  SNIPPET: snippets/product-card.liquid
  
  USAGE in any section or template:
    {% render 'product-card', product: product, show_badge: true %}
  
  VARIABLES you can pass:
    product      — (required) Shopify product object
    show_badge   — (optional) true/false — show SALE badge, default true
    marquee_text — (optional) override marquee string, default uses metafield or fallback
  ============================================================
{% endcomment %}

{% assign show_badge = show_badge | default: true %}

{% comment %} Calculate discount % {% endcomment %}
{% if product.compare_at_price > product.price %}
  {% assign discount_amount = product.compare_at_price | minus: product.price %}
  {% assign discount_pct = discount_amount | times: 100 | divided_by: product.compare_at_price %}
{% else %}
  {% assign discount_pct = 0 %}
{% endif %}

{% comment %} Marquee text — use metafield OR section setting OR fallback {% endcomment %}
{% assign marquee_text = marquee_text | default: product.metafields.custom.marquee_text | default: 'Flash sale · New arrival · Limited stock · Hot Seal' %}
{% assign marquee_items = marquee_text | split: '·' %}

<div class="pc-card" data-product-id="{{ product.id }}">

  {% comment %} ── Top info ── {% endcomment %}
  <div class="pc-top">
    <div class="pc-title-row">
      <span class="pc-title">{{ product.title }}</span>
      {% if show_badge and discount_pct > 0 %}
        <span class="pc-badge">SALE -{{ discount_pct }}%</span>
      {% endif %}
    </div>

    <div class="pc-price">
      {% if product.compare_at_price > product.price %}
        <span class="pc-price-orig">{{ product.compare_at_price | money }}</span>
      {% endif %}
      <span class="pc-price-sale">{{ product.price | money }}</span>
    </div>
  </div>

{% comment %} ── Product image (hover/click mode) ── {% endcomment %}
{% assign pc_media_type = section.settings.card_media_type | default: 'hover' %}
{% assign pc_hover_style = section.settings.image_hover_style | default: 'slide' %}

<a href="{{ product.url }}" class="pc-image-link" aria-label="{{ product.title }}">
  <div class="pc-image pc-image--media" data-media-type="{{ pc_media_type }}" data-hover-style="{{ pc_hover_style }}">
    <div class="pc-image-inner">

      {% if pc_media_type == 'click' %}
        {% comment %} CLICK MODE {% endcomment %}
        <div class="pc-click-track">
          {% for media in product.media limit: 5 %}
            <img
              class="pc-click-slide {% if forloop.first %}is-active{% endif %}"
              src="{{ media | image_url: width: 480 }}"
              alt="{{ media.alt | default: product.title }}"
              loading="lazy"
              width="480" height=""
            >
          {% else %}
            {{ 'product-1' | placeholder_svg_tag: 'pc-placeholder-svg' }}
          {% endfor %}
        </div>

        {% if product.media.size > 1 %}
          <button class="pc-nav pc-nav--prev" data-nav="prev" aria-label="Previous">
            <svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M6.5 2L3.5 5l3 3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </button>
          <button class="pc-nav pc-nav--next" data-nav="next" aria-label="Next">
            <svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M3.5 2L6.5 5l-3 3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </button>
          <div class="pc-dots">
            {% for media in product.media limit: 5 %}
              <span class="pc-dot {% if forloop.first %}is-active{% endif %}"></span>
            {% endfor %}
          </div>
        {% endif %}

      {% else %}
        {% comment %} HOVER MODE {% endcomment %}
        {% if product.media.size > 0 %}
          <img
            class="pc-img pc-img--primary"
            src="{{ product.media[0] | image_url: width: 480 }}"
            alt="{{ product.media[0].alt | default: product.title }}"
            loading="lazy"
            width="480" height=""
          >
          {% if product.media.size > 1 %}
            <img
              class="pc-img pc-img--secondary"
              src="{{ product.media[1] | image_url: width: 480 }}"
              alt="{{ product.media[1].alt | default: product.title }}"
              loading="lazy"
              width="480" height=""
            >
          {% endif %}
        {% else %}
          {{ 'product-1' | placeholder_svg_tag: 'pc-placeholder-svg' }}
        {% endif %}
      {% endif %}

    </div>
  </div>
</a>

  {% comment %} ── Black marquee bar ── {% endcomment %}
  <div class="pc-marquee-bar" aria-hidden="true">
    <div class="pc-marquee-track">
      {% comment %} Render items twice for seamless infinite loop {% endcomment %}
      {% for i in (1..2) %}
        {% for item in marquee_items %}
          {% assign label = item | strip %}
          {% if label != '' %}
            <span class="pc-marquee-item">
              {{ label }}<span class="pc-marquee-dot"></span>
            </span>
          {% endif %}
        {% endfor %}
      {% endfor %}
    </div>
  </div>

  {% comment %} ── Hover overlay ── {% endcomment %}
  <div class="pc-overlay" aria-hidden="true">
    <button
      class="pc-overlay-btn pc-quick-view"
      data-product-handle="{{ product.handle }}"
      data-product-url="{{ product.url }}"
      type="button"
    >
      Quick view
    </button>

    <button
      class="pc-overlay-btn pc-add-bag"
      data-variant-id="{{ product.selected_or_first_available_variant.id }}"
      data-product-title="{{ product.title | escape }}"
      type="button"
    >
      Add to bag
    </button>
  </div>

</div>
