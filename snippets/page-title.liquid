<div class="nv-page-header">
  <h1 class="nv-page-title">{{ title | default: 'Collections' }}</h1>

  {%- if subtitle != blank -%}
    <div class="nv-page-subtitle">{{ subtitle }}</div>
  {%- endif -%}
</div>